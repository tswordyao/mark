```
过往成果：

stp	simple template，支持递归

paging 可缓存分页器

resumable.js h5文件上传组件

bigbang 兼顾iframe和单页模式的前端开发框架

wiget和behavior 高度抽象的组件系统

plain-table 根据json生成合并头和行的表格

图形化拖拽树形结构生成表头

profile中的晦涩bug ——call.bind和reduce

bemify  简单的css scope集合bem和 r-class

hub 简单的package级store和eventBus的生成器

pick解构和deepGet 常见的数据子集操作

playerUi，流程劫持+数据驱动+事件交互的自定义UI设计

NUI，  every entry+final output，两个阶段AST插件配合的构建设计

给nej、regular、dubbo.js都提过pull-request

HESSIAN 精读，解析http和socket共存的不矛盾性

URS、auth、cookie-session的独立或与java统一设置

直播追帧实现低延时


```