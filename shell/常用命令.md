bash常用
```
Basically bash is sh, with more features and better syntax.  Most commands work the same, but they are different.

http://www.runoob.com/linux/linux-command-manual.html


指定命令解释器 shebang
#!/bin/bash


查看ip
 ifconfig en0 | grep inet | sed s/inet// |sed s/netmask.*$//
或 ifconfig en0|grep -E "([0-9]{1,3}\.){3}[0-9]"


显示本机远程连接情况
netstat  -an |grep '10.104.11.107'

返回到上一个目录位置
cd - 和 cd $OLDPWD 
cd - 可以在两个位置来回切换


#给当前用户的file文件添加可执行权限
chmod u+x file

查看文件夹大小
du -sk ./pictures
du命令(disk usage)用来展示磁盘使用量的统计信息。du和df算是一对同门师兄弟，du侧重在文件夹和文件的磁盘占用方面，而df侧重在文件系统级别的磁盘占用方面

查看当前目录下所有子目录和文件的大小
du -sh *

管道:
|    表示将前一个命令的返回结果用作后一个命令的参数
>   表示如果没有文件就创建新文件并写入内容，如果文件存在将原文件清空并写入内容;
>> 表示如果文件不存在就创建新文件并写入内容，如果文件存在就将将内容追加到原文件
管道命令必须要能够接收来自前一个命令的数据成为standard input继续处理才行。

sed -n Np 文件名
其中，“N”必须替换成一个数字，例如，你要取文件1.txt的第7行，命令为： sed -n 7p  hi.txt


http://man.linuxde.net/grep
grep  "正则表达式"   file   file2   file3  可以接多个
grep  字符串  file_name
grep 搜索内容 目录 -r
grep 搜索内容 ./ -r --include *.{php,html}  #只在.php和.html文件中递归搜索
grep 搜索内容 ./ -r --exclude "README"   #排除所有README文件
grep 搜索内容 ./ -r --exclude-from filelist  #排除filelist文件列表里的文件

cat get.json # 查看文件内容

uptime命令显示了系统运行了多久以及目前登录的用户有多少,另外还显示了间隔1分钟、5分钟和15分钟的负载平均值

expand命令用于将文件的制表符（TAB）转换为空白字符（space），将结果显示到标准输出设备。
expand -t2 list.json | read input; echo "result is: $input"  #read只能读到第一行
除非写复杂base脚本, 循环读取多行 http://www.runoob.com/linux/linux-comm-read.html
简单的正确实现:
expand my.json > file   但是如果file就是my.json的话, 会被清空, 而使用>>的话,原有的内容之后追加了新的内容

rmdir 空目录
rm -rf 目录名字
rm -f 文件名

wc -l  (统计列数)
wc -w (统计词数)
wc -c (统计总字数)

du  目录名 
du -s    目录名  (只看总大小)
du -sh  目录名  (只看总大小 且提高可读性表示 )
du -s /usr/* | sort -rn  按字节大小倒序排列  (如果sh是按数字不看单位, 有坑, 1G排在2M后面)


ls -a | wc  列出所有子目录或文件, 再统计其个数
ls -lR|grep "^d"|wc -l 
说明：ls -l  长列表输出该目录下文件信息(注意这里的文件，不同于一般的文件，可能是目录、链接、设备文件等)
          grep "^-"  这里将长列表输出信息过滤一部分，只保留一般文件，如果只保留目录就是 ^d


#echo 和 read 交互
echo -n "Please enter your name:";
read NAME;
echo "Hello $NAME"!


*  grep 更适合单纯的查找或匹配文本
*  sed 更适合编辑匹配到的文本
*  awk 更适合格式化文本，对文本进行较复杂格式处理


判断有无git将结果通过dec/null过滤为无输出只有成功或失败的信号
which git > /dev/null && git -v    
which git > /dev/null || svn -v  

 echo   node版本是$(node -v)   ,  npm版本是$(npm -v)


#shell中的展开运算符
echo 字母{w..f}
echo a{A{1,2},B{3,4}}b
mkdir {2007..2009}-0{1..9} {2007..2009}-{10..12}
ls s* 会列出所有s开头的目录下的子内容
echo s* 会列出所有s开头的目录本身

单引号，禁止所有的展开我们使用。 
双引号，除了 $  \ 和 `， 其他特殊字符当作普通字符来看待。 
这意味着单词分割，路径名展开， 波浪线展开，和花括号展开都被禁止——然而参数展开echo "$USER"，算术展开echo "$((2+3))"，和命令替换echo "$(ls)"仍然执行。


#一些快捷键
Ctrl+L 清屏  cLear?

Ctrl+a  光标移动到命令行首  ahead的意思?
Ctrl+e  光标移动到命令行尾  end?

Ctrl+w  删除光标所在位置前面的一段(各段是由空格分割的) word?
Ctrl+u  删除行首到光标所在位置的所有  util?
Ctrl+k  删除光标所在位置到行尾的所有  keep?
Ctrl+y  粘贴Ctrl+w或Ctrl+u或Ctrl+k删除的内容到当前光标位置  yesterday?

Ctrl+d 删除(逐步往后)
Ctrl+h 删除(逐步往前)  这个没用,和delete键一样
Ctrl+f  把当前光标往后移一位 这个没用,和方向键一样
Ctrl+b  把当前光标往前移一位 这个没用,和方向键一样
Ctrl+x  把当前光标从当前位置和行首来回切换
Ctrl+t  把当前光标字母往后移一位
Ctrl+z  把命令放入后台 这个没发现有用
Ctrl+r  在历史命令中搜索  这个很有用, 输入关键字找出命令 ,fish下不能


#获取当前git分支
branch=`git branch | awk -F " " '{print $2}'`


用来查看在 Linux 主机中使用了多少虚拟内存(以下四句可以合并为一行): 
 RAM=`free | grep ^Mem | awk '{print $2}'` ; 
Swap=`free | grep ^Swap | awk '{print $2}'` ;
 echo "RAM = $RAM and Swap = $Swap" ;
 echo "Total Virtual memory is $((RAM+Swap))" ;

```


```
Ctrl + L 来清除屏幕（而不是键入 clear）。
sudo !! 以 sudo 特权运行先前的命令。
grep -Ev '^#|^$' <file> 将显示文件内容，不带注释或空行。” 


cd -    返回到上一个目录位置
webstorm 是 Commond+alt+键盘方向左键 <—
vscode 是 ctrl -  (只在mac上)


“对我来说，如果我正在使用终端文本编辑器，并且希望将其丢开，以便可以快速执行其他操作，则可以使用 Ctrl + Z 将其放到后台，接着执行我需要做的一切，然后用 fg 将其带回前台。有时我也会对 top 或 htop 做同样的事情。我可以将其丢到后台，并在我想检查当前性能时随时将其带回前台。我不会将通常很快能完成的任务在前后台之间切换，它确实可以增强终端上的多任务处理能力。”
 —Jay LaCroix



“我经常在某一天在终端中做很多相同的事情，有两件事是每天都不变的：

Ctrl + R 反向搜索我的 Bash 历史记录以查找我已经运行并且希望再次执行的命令。
插入号（^）替换是最好的，因为我经常做诸如 sudo dnf search <package name> 之类的事情，然后，如果我以这种方式找到合适的软件包，则执行 ^search^install 来重新运行该命令，以 install 替换 search。


“可能是用 ssh -X 来在其他计算机上运行图形程序。（在某些终端仿真器上，例如 gnome-terminal）用 C-S c 和 C-S v 复制/粘贴。我不确定这是否有价值（因为它有趣的是以 ssh 启动的图形化）。最近，我需要登录另一台计算机，但是我的孩子们可以在笔记本电脑的大屏幕上看到它。这个链接向我展示了一些我从未见过的内容：通过局域网从我的笔记本电脑上镜像来自另一台计算机屏幕上的活动会话（x11vnc -desktop），并能够同时从两台计算机上进行控制。” —Kyle R. Conway

“你可以安装 sl（$ sudo apt install sl 或 $ sudo dnf install sl），并且当在 Bash 中输入命令 sl 时，一个基于文本的蒸汽机车就会在显示屏上移动。” —Don Watkins

```