```
Ubuntu

添加第三方地址：
sudo add-apt-repository "deb http://archive.canonical.com/ lucid partner"
sudo apt-get update


解决The program 'make' can be found in the following packages:
sudo apt-get install build-essential
```