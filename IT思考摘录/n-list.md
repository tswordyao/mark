# -- n list

```
varnish
GSLB
LSS LMS MPS

geogebra

alfred xshell iTerm2 macVim PD9虚拟机

Selenium WebDriver ChromeDriver

kibana  knative CNCF Kubernetes 

OSPF LVS NIO Netty flink 时序数据库 neo4j cypher yarn jupyter

应用治理  高可用 高并发 高吞吐

vscode-remote-ssh

wireshark抓包工具 对比其他 Charles windows proxy和mac proxy  fiddler  raven.js

srping, spring mvc, spring boot, spring cloud 微服务 服务发现

dubbo，hessian，jetty，netty，nginx，docker，eureka， consul,  Hadoop、Hbase、Storm、Kafka

maven，Service Mesh

webWorker serviceWorker

将服务发布到k8s集群中，同是采用 NodePord方式 暴露端口出去，不用通过网关就可以访问

webGl, threejs，d3, d3+react 骨骼动画 第一人称控制

grpc,  jsonrpc, Thrift

redux, mbox, rxjs  react-hooks  vue3

OSS

acl  envoy  康威定律 monolithic   service-mesh prana synapse和nerve

typescript  infer type

ssr，next，nuxt,

node clinic

koa2 nestjs

swagger

kafka

zookeeper

rabbitMq

IoT设备 物联网

flv，mpeg，hls，rtmp

Travis CI，CD  && Github Pages ，Jenkins

cas和openid 单点登录sso，OAuth2，jwt

Elasticsearch 和 solar（solr） Lucene

Flink是由Apache软件基金会开发的开源流处理框架,其核心是用Java和Scala编写的分布式流数据流引擎

数据库代理

综合性IDE，初始化脚手架、语法提示、编译调试、发布

基础的深入：登录鉴权、文件服务、中间件

既然是集群，就不能够出现单点故障，如果大家关注云服务，可能会接触到以下词汇，“双机热备”，“两地三中心”等等词汇。

ACK (Acknowledgement）即是确认字符，在数据通信中，接收站发给发送站的一种传输类控制字符。表示发来的数据已确认接收无误。在TCP/IP协议中，如果接收方成功的接收到数据，那么会回复一个ACK数据。通常ACK信号有自己固定的格式,长度大小
```